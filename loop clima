function loop() {
    if(!gameActive) return;
    frame++;
    ctx.save();
    
    // 1. Shake e Limpeza
    if(shake > 0) { ctx.translate((Math.random()-0.5)*10, (Math.random()-0.5)*10); shake--; }
    ctx.clearRect(0,0,canvas.width,canvas.height);

    // 2. DESENHAR CLIMA 
    updateWeather();

    // 3. Física e Lógica (Inimigos, Jogador, etc)
    // ... restante do código ...
